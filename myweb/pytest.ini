[pytest]
DJANGO_SETTINGS_MODULE = myproject.settings_test
addopts = -ra
testpaths = tests
markers =
    unit: pruebas unitarias
    integration: pruebas de integración
    e2e: flujos end-to-end
    security: pruebas de permisos y amenazas
    performance: pruebas de estrés y concurrencia
